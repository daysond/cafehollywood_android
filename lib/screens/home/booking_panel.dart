import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';

class BookingPanel extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('booking panel'),
    );
  }
}
